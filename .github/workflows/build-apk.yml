name: Build APK

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          java-version: 21
          distribution: temurin

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Auto-increment version
        id: version
        run: |
          CURRENT=$(node -p "require('./package.json').version")
          # Incrementa patch: 1.0.0 -> 1.0.1
          IFS='.' read -r MAJOR MINOR PATCH <<< "$CURRENT"
          NEW_PATCH=$((PATCH + 1))
          NEW_VERSION="$MAJOR.$MINOR.$NEW_PATCH"
          echo "version=$NEW_VERSION" >> $GITHUB_OUTPUT
          echo "Versão: $CURRENT -> $NEW_VERSION"

          # Atualiza package.json
          node -e "
            const fs = require('fs');
            const pkg = JSON.parse(fs.readFileSync('package.json', 'utf8'));
            pkg.version = '$NEW_VERSION';
            fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
          "

          # Atualiza version.json
          node -e "
            const fs = require('fs');
            const v = JSON.parse(fs.readFileSync('public/version.json', 'utf8'));
            v.version = '$NEW_VERSION';
            fs.writeFileSync('public/version.json', JSON.stringify(v, null, 2) + '\n');
          "

      - name: Install dependencies
        run: npm ci

      - name: Build web assets
        run: npm run build

      - name: Sync Capacitor
        run: npx cap sync android

      - name: Make gradlew executable
        run: chmod +x android/gradlew

      - name: Build APK
        run: cd android && ./gradlew assembleDebug

      - name: Rename APK
        run: mv android/app/build/outputs/apk/debug/app-debug.apk android/app/build/outputs/apk/debug/PhysiqCalc.apk

      - name: Upload APK as artifact
        uses: actions/upload-artifact@v4
        with:
          name: PhysiqCalc
          path: android/app/build/outputs/apk/debug/PhysiqCalc.apk
          retention-days: 30

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.version }}
          name: PhysiqCalc v${{ steps.version.outputs.version }}
          body: |
            ## PhysiqCalc v${{ steps.version.outputs.version }}

            APK gerado automaticamente.

            **Download:** Baixe o `PhysiqCalc.apk` abaixo.
          files: android/app/build/outputs/apk/debug/PhysiqCalc.apk
          generate_release_notes: true

      - name: Commit version bump
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add package.json public/version.json
          git diff --staged --quiet || git commit -m "chore: bump version to v${{ steps.version.outputs.version }} [skip ci]"
          git push
